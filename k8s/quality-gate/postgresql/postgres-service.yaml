apiVersion: v1
kind: Service
metadata:
  name: postgres-sonar-service
  namespace: kube-system
spec:
  selector:
    app: postgres-sonar
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
